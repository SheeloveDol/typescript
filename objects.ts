const User = {
	name: 'Red',
	email: 'red@mail.com',
	isActive: true,
}

const createUser = ({name: string, isPaid: boolean}) => {
	// Do something here, like return the object
}

const createSession = (): {} => {
    return {}
}

createUser({name: 'Red', isPaid: true})









export {}